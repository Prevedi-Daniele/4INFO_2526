function forInRicreato(oggetto){
    let chiaviOggetto = Object.keys(oggetto)
    
    for (let i = 0; i < chiaviOggetto.length; i++) {
        nomeChiave = chiaviOggetto[i]
        
        // Codice da eseguire nel for qui
    }
}